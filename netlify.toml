[build]
  base = "frontend"
  command = "npm run build"
  publish = "dist"

# SPA fallback: the app now has real client-side routes (/watch/:id, /login, etc.)
# - without this, a direct visit or refresh on any route but "/" 404s.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
